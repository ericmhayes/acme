# Terraform state and working dirs
.terraform/
*.tfstate
*.tfstate.backup
.terraform.tfstate.lock.info

# Local override files (never commit machine-local overrides)
*.override.tf
*.override.tf.json
override.tf
override.tf.json

# Crash logs
crash.log
crash.*.log

# OS / editor cruft
.DS_Store
*.swp

# NOTE: .terraform.lock.hcl is intentionally committed (provider pinning).
#       Do NOT add it here.
